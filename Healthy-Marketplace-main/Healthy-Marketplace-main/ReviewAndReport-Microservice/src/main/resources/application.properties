# ReviewReportService local fallback properties
# Load config from Config Server if available
spring.config.import=optional:configserver:${CONFIG_SERVER_URL:http://localhost:8889}

# Fallback values for local development
spring.application.name=ReviewReportService
server.port=8084

# Enable debug output for the Config client
logging.level.org.springframework.cloud.config.client=DEBUG
